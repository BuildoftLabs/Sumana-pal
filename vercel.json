{
  "buildCommand": "vite build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/blogs/:slug", "destination": "/api/blog/[slug]" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/blog/[slug].js": {
      "maxDuration": 10
    }
  }
}
